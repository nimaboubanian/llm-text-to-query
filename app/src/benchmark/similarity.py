"""SQL similarity evaluation module (stub for future implementation)."""


def evaluate_sql_similarity(
    generated_sql: str,
    reference_sql: str,
    generated_result: list[dict],
    reference_result: list[dict],
) -> dict:
    """Evaluate similarity between generated and reference SQL.

    This is a STUB implementation for future development.
    Eventually will return metrics like:
    - Syntax similarity
    - Semantic similarity
    - Result set overlap (Jaccard)

    Args:
        generated_sql: SQL generated by LLM
        reference_sql: Ground truth SQL
        generated_result: Results from executing generated SQL
        reference_result: Expected results from reference file

    Returns:
        Dictionary with similarity metrics (scaffold for future)
    """
    # STUB: Just return empty dict for now
    # TODO: Implement SQL similarity evaluation
    return {}
