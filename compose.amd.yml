# GPU override for AMD GPUs (ROCm).
# Usage: docker compose -f compose.yml -f compose.amd.yml up -d
#
# Prerequisite: AMD ROCm drivers
#   https://rocm.docs.amd.com/projects/install-on-linux/en/latest/

services:
  ollama:
    # Pinned to match compose.yml's daemon version; `rocm` alone floats.
    image: ollama/ollama:0.32.6-rocm
    devices:
      - /dev/kfd
      - /dev/dri
